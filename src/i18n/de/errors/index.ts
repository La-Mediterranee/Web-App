import type { BaseTranslation } from '../../i18n-types';

const de_errors: BaseTranslation = {
	404: 'Die gesuchte Seite existiert leider nicht',
	500: 'Es gab einen Fehler auf unserem Server',
};

export default de_errors;
